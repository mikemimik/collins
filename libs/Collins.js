/**
 * Collins Module
 * @module collins
 * @see module:collins
 */

'use strict';

class Collins {
  constructor(config) {

    this.services = [];
    this.Runtime = require('../utils/Runtime');
    // TODO: check if config is valid
  }

  use(serviceGear) {

    // TODO: add service to array of services
  }
}

module.exports = Collins;